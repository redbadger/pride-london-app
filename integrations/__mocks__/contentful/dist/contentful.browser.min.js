const mockEventData = {
  items: []
};

// eslint-disable-next-line import/prefer-default-export
export const createClient = () => ({
  async getEntries(opts) {
    return opts.content_type === "event" ? mockEventData : {};
  }
});
